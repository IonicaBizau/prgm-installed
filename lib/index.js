"use strict";

/**
 * prgmInstalled
 * Check if a program is available on the system.
 *
 * @name prgmInstalled
 * @function
 * @param {Number} a Param descrpition.
 * @param {Number} b Param descrpition.
 * @return {Number} Return description.
 */
module.exports = function prgmInstalled (a, b) {
    return a + b;
};